{
  "manifest_version": 3,
  "name": "Copy Edit — inline diff",
  "version": "0.1.0",
  "description": "Click to edit any page text inline, then toggle a Review mode that shows an inline diff (insertions/deletions) of your changes. Export a change report.",
  "action": {
    "default_title": "Copy Edit: toggle inline editing on this page"
  },
  "background": {
    "service_worker": "background.js"
  },
  "permissions": ["scripting", "activeTab", "storage"],
  "host_permissions": ["http://*/*", "https://*/*"]
}
